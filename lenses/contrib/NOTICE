go-augeas-original contrib lenses
==================================

The `.aug` files in this directory are **original lenses authored for
go-augeas**. They are NOT part of the upstream Augeas distribution and must
not be confused with the verbatim upstream mirror under `lenses/dist/`.

They cover configuration formats that upstream Augeas 1.14.1 does not yet
ship a lens for:

    Wireguard  (lens Wireguard.lns)  WireGuard tunnels: /etc/wireguard/*.conf
    Rclone     (lens Rclone.lns)     rclone remotes:    rclone.conf
    Caddyfile  (lens Caddyfile.lns)  Caddy web server:  /etc/caddy/Caddyfile
    Nftables   (lens Nftables.lns)   nftables ruleset:  /etc/nftables.conf
    Unbound    (lens Unbound.lns)    Unbound resolver:  /etc/unbound/unbound.conf

Caddyfile heredocs (`<<MARKER`) and literal empty blocks (`dir { }`) are out of
scope for a regular lens; heredocs are rejected at the go-augeas API layer
rather than silently misparsed. Nftables brace groups wrapped across multiple
physical lines (a long `elements = { a,` / `b }` list as nft may print it) are
likewise rejected at the API layer rather than silently misparsed. See the
README for the full boundary notes.

Each lens carries inline `test ... get ... =` / `test ... put ... after =`
assertions that are run in CI (see internal/interp TestContribCorpus), exactly
like the upstream corpus is gated on its own `test` assertions by TestCorpus.
Their `Util`/`IniFile`/`Sep`/`Rx` imports resolve from the upstream
`lenses/dist/` corpus.

LICENSE
-------

These lens sources are licensed under the **GNU Lesser General Public
License, version 2.1 or later** (see `../dist/LICENSE.lenses`), the same
license as the upstream Augeas corpus, so that they compose with the corpus
they import from. They keep their own LGPL header.

This LGPL license applies ONLY to the `.aug` files under `lenses/`. The rest
of the go-augeas/augeas repository (the pure-Go interpreter and API) is
licensed under the BSD-3-Clause license (see the top-level `LICENSE`).
